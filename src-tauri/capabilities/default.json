{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core application window only; file access is limited to custom commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
